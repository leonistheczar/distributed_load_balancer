# Achievements - 2026/04/20

## Live CLI Dashboard Integration and Stability
- Integrated `LiveDashboard` into `src/cli/main.py` with robust import fallback for both:
  - module mode (`python -m src.cli.main`)
  - script mode (`python src/cli/main.py`)
- Replaced fragile dashboard metric wiring with stable in-loop counters:
  - dispatched, dropped, reroutes, failovers, cache hits.
- Added safe loader row-count probing (`_safe_total_rows`) to avoid runtime failures when total row metadata is unavailable.

## Dashboard UX Refactor (Professional, Progressive Layout)
- Refactored `src/cli/live_dashboard.py` into a clearer operations-style layout:
  - `Simulation Overview`
  - `Traffic & Reliability`
  - `Node Execution Matrix`
  - `Replay Evidence`
- Improved readability and hierarchy with stronger KPI grouping and cleaner metric labels.
- Added adaptive layout behavior for narrow/wide terminal widths.
- Added Windows-friendly UTF/ASCII-safe rendering behavior to reduce console compatibility issues.

## Node-Level Visibility Enhancements
- Ensured per-node execution visibility during runtime with:
  - health state (`up/down`)
  - in-flight requests
  - handled requests
  - progress percentage per node
  - errors
  - avg latency
  - p95 latency
  - load indicator (where layout permits)
- Added high-level node snapshot indicators in overview/KPI context:
  - healthy vs failed count
  - busiest node
  - slowest node (p95 basis)

## Dataset Replay Evidence in CLI
- Added explicit replay proof panel (`Replay Evidence`) to demonstrate requests are being streamed from dataset rows:
  - replay source
  - dataset cursor index
  - dataset-derived simulated time
  - last row timestamp
  - last request method/path
  - arrival delta used by replay timing
- Wired replay metadata from `main.py` into dashboard tick updates each dispatch cycle.

## Results Folder Refactor (Clutter Reduction)
- Reworked output directory strategy into classified hierarchy under `results/`:
  - `results/<dataset>/algorithms_<algo>/sample_<n>/queue_<cap>/bots_<mode>/`
- Refactored exporter outputs into run-scoped subfolders:
  - `runs/<timestamp>/reports/`
  - `runs/<timestamp>/charts/`
  - `runs/<timestamp>/html/`
- Added high-resolution timestamping (`%Y%m%d_%H%M%S_%f`) and collision guard loop to ensure unique run folders and prevent overwrite conflicts.

## Validation Performed
- Repeated lint checks on touched CLI/metrics files with no new lint errors.
- Multiple smoke runs executed successfully after each major change.
- Verified runtime output includes:
  - node health/metrics during execution
  - replay evidence fields
  - classified, timestamped result artifact paths.
